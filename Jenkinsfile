pipeline {
	agent any

    stages {
    	stage('Ansible prerequisites') {
	        steps {
	            script {
	            	sh '''
	                	echo 11111
                    '''
	            }
	        }
	    }
    }
}